# sharksfin-store

a spearfishing website project

## Wetsuit image assets

Some environments reject binary files in commits. For wetsuit images, this repo stores source URLs instead of binaries in `images/wetsuit-source-urls.txt`.

To fetch those images locally into `/images`, run:

```bash
./scripts/download-wetsuit-images.sh
```
